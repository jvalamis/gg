{
  "name": "game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "http-server . -p 8080",
    "dev": "http-server . -p 8080 -c-1 --cors",
    "build": "mkdir -p dist && cp -r src dist/ && cp index.html dist/ && cp package.json dist/ && cp config.js dist/"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "phaser": "^3.87.0",
    "peerjs": "^1.5.2"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}