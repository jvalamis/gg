{
  "name": "game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "http-server src -p 8080",
    "dev": "http-server src -p 8080 -c-1 --cors",
    "build": "mkdir -p dist && cp -r src/* dist/ && cp package.json dist/ && cp config.js dist/"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.3.1",
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}