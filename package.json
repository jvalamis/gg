{
  "name": "game",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "mkdir -p dist && cp -r src/* dist/ && cp package.json dist/ && cp config.js dist/"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.3.1",
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}